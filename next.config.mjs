/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "img.freepik.com",
      "prod-img.thesouledstore.com",
      "res.cloudinary.com"
    ],
  },
};

export default nextConfig;
